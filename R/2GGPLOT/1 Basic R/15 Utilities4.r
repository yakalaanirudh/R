#UTILITIES MERGING TWO DATAFRAMES

#merge(x,y,by="colname")
#x=dataframe
#y=dataframe
#by=by variable we want to merge for



#merge(x,y,by.x="colname",by.y="colname")
#x=dataframe
#y=dataframe
#by.x=by variable we want to merge for in x
#by.y=by variable we want to merge for in y


#If there is more than one match,all possible matches contribute one row each


#we can use
#inner_join()
#left_join()
#right_join()
#full_join()